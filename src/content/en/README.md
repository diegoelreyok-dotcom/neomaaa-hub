# English Content

This folder will hold English translations of the Spanish content.
Currently empty — translations will be generated incrementally.

Fallback behavior: when a user with `lang=en` accesses a doc that doesn't exist
in this folder, the Spanish version is shown with a banner indicating that the
English translation is pending.
