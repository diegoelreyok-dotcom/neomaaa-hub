node_modules/
.next/
.env.local
.vercel
.env*.local

# Build-generated search index (SEC1 — not public, never committed)
data/search-index.json

# Legacy: ensure stale copy can't accidentally be committed again
public/search-index.json
